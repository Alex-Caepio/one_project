type: object
properties:
  message:
    type: string
    example: The given data was invalid.
  errors:
    type: object
    example:
      new_schedule_id:
       - This schedule does not belong to the service.
